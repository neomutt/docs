# How to Use a Password Manager [TEMPLATE]

:::{admonition} Diátaxis: How-To Guide
:class: note

Write as **directions**. Assume the reader is competent and knows what they want to achieve.
Be practical and goal-focused. Use numbered steps for procedures. Don't explain why — link
to explanation pages instead. Keep it focused on the specific task. Start with prerequisites,
give the steps, show the expected result.
:::

:::{admonition} Content needed
:class: warning

- Overview of password manager integration approaches
- Using `pass` (password-store) with NeoMutt
- Using `gpg` encrypted password files
- Using `secret-tool` / GNOME Keyring
- Using `security find-generic-password` on macOS
- Configuring `account_command` for password retrieval
- Configuring `imap_pass`, `smtp_pass` with backtick commands
- Security considerations for password caching
:::

## Prerequisites

## Using `pass` (password-store)

## Using GPG-Encrypted Password Files

## Using GNOME Keyring / `secret-tool`

## Using macOS Keychain

## Configuring `account_command`

## Using Backtick Commands for `imap_pass` and `smtp_pass`

## Security Considerations
